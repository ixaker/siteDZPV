export type Translations = {
  locale: string;
  menu: { name: string }[];
  homePage: { title: string };
  btnSend: string;
  aboutFactory: {
    title: string;
    image: string;
    paragraphs: string[];
  };
  ourClients: { title: string };
};
